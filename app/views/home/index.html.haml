%h1 Home#index
%p Find me in app/views/home/index.html.haml

%h2 Enter Company Details:
= form_for @company, :class => "form-horizontal", :role => "form" do |f|
  .form-group
    = f.label :name, :class => "control-label"
    = f.text_field :name, :class => "form-control", :placeholder => "Company Name"
  .form-group
    = f.submit :class => "btn btn-default"
